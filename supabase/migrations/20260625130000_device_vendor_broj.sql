-- ============================================================
-- 브로제이(Broj) 출입·안면인식 단말기 vendor 추가
-- ------------------------------------------------------------
-- 선릉점 출입은 브로제이 안면인식 CRM 이 담당한다.
-- AccessDeviceAdapter 추상화에 'broj' 어댑터를 끼우기 위한 enum 값 추가.
-- ⚠ enum ADD VALUE 는 같은 트랜잭션에서 사용 불가 → 별도 마이그레이션으로 분리.
-- ============================================================
ALTER TYPE device_vendor ADD VALUE IF NOT EXISTS 'broj';
